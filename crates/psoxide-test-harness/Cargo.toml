[package]
name = "psoxide-test-harness"
version = "0.1.0"
edition = "2024"

[dependencies]
psoxide-core = { path = "../psoxide-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
